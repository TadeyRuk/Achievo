{
  "name": "achievo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "cd frontend && npm install && npm run build",
    "check-contract-integration": "node scripts/check-contract-integration.mjs",
    "export-payout-proof": "node scripts/export-payout-proof.mjs --write"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^13.3.0",
    "@upstash/redis": "^1.38.0"
  },
  "devDependencies": {
    "@vercel/node": "^5.1.5",
    "typescript": "~6.0.2"
  }
}
